package zm.mud.core.trigger.service;

import org.springframework.stereotype.Service;

import zm.mud.core.trigger.matcher.IMatcher;

@Service
public class MatcherService {
 
    public void registerMatcher(IMatcher matcher){
        
    }
}
